'use strict';

module.exports = function(environment) {
    let ENV = {
        modulePrefix: 'movies-app',
        environment: environment,
        locationType: 'auto',
        rootURL: '/'
    };

    return ENV;
};